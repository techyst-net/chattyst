# Generated by .brand/gen_repo_build.py — regenerate rather than hand-edit.
#
# Builds this app's images from the source in this directory. Used by ./build.sh;
# the server never needs this file, only compose.yaml.

name: techyst-09-chatwoot
services:
  rails:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-chatwoot-rails:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.techyst
      cache_from:
      - ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-chatwoot-rails:latest
  sidekiq:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-chatwoot-sidekiq:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.techyst
      cache_from:
      - ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-chatwoot-sidekiq:latest

# Generated by .brand/gen_repo_compose.py — regenerate rather than hand-edit.
#
#   infisical export --projectId techyst-09-chatwoot --env prod --format dotenv > .env
#   docker compose up -d
#
# Images come from ghcr.io; `docker login ghcr.io` first. Databases, cache,
# queue and object storage are all external, so nothing here holds state and
# `docker compose down` is safe.

name: techyst-09-chatwoot
services:
  rails:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-chatwoot-rails:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - bundle
    - exec
    - rails
    - s
    - -p
    - '3000'
    - -b
    - 0.0.0.0
    ports:
    - 3010:3000
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  sidekiq:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-chatwoot-sidekiq:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    command:
    - bundle
    - exec
    - sidekiq
    - -C
    - config/sidekiq.yml
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
